# 버전 기록

## v0.9.0 — 2026-08-04

### 바뀜

- **건물이 정사각형을 벗어났다.** 지금까지 평면도는 늘 격자를 꽉 채운 정사각형이라 사건마다 방 크기만 달랐다. 이제 실루엣 마스크(정사각·ㄱ자·ㄷ자·대각·도넛)를 시드로 골라 격자에서 칸을 도려낸다. 판이 클수록 자주 나온다 — 보통은 열에 여섯, 어려움부터는 열에 여덟 사건이 정사각형이 아니다. 쉬움(4×4)은 방 개수가 빠듯해서 그대로 정사각형이다.
- **안뜰과 연못.** 도려낸 칸이 건물 안에 갇히면 저택은 안뜰 ⛲, 농장은 연못 💧 이 된다. 바깥과 이어지면 그냥 건물 밖의 빈 땅이다. 어느 쪽이든 사람이 설 수 없어서 `옆` 이 닿는 범위가 사건마다 달라진다. 이름표는 점선이라 방 이름과 헷갈리지 않는다.
- **가장 굵은 벽선이 건물 모양을 따라간다.** 보드를 두르던 사각 테두리를 없애고 외벽을 칸이 그리게 했다. 그래서 ㄱ자 건물은 맨 바깥 선도 ㄱ자고, 도려낸 자리에는 종이가 그대로 비친다. 안뜰도 건물에 뚫린 구멍이라 같은 굵기의 벽을 받는다.
- **새 증언 — `난 서재에서 막 나온 참이었어!`** 그 방과 벽을 맞댄, 그 방이 **아닌** 칸을 뜻한다. 퍼즐에 처음으로 시간이 들어왔고, 논리적으로는 `옆` 이 못 넘던 방 경계를 넘는 첫 규칙이다. 방 사이는 늘 오갈 수 있다고 본다 (문이 그려져 있지 않아도).

### 알아둘 것

- **기존 시드의 사건이 달라진다.** 평면도를 만드는 순서가 바뀌어 같은 시드라도 다른 사건이 나온다. 기록에 남은 제목이 실제 사건과 어긋날 수 있다. 점수와 순위는 `시드 + 난이도` 기준이라 그대로다.

## v0.8.3 — 2026-08-04

### 더함

- **처음 온 사람에게 화면을 직접 짚어준다.** 규칙 패널은 처음부터 있었지만 줄글이라 아무도 안 읽었다. 이제 첫 방문에 스포트라이트가 뜬다 — 평면도 → 증언 → 범례 → 지목 → 브리핑 순으로 화면을 돌며 그 자리만 밝히고 거기서 할 말을 한다. 증언 목록 아래 `?` 로 언제든 다시 열고, 아무 데나 눌러 넘기거나 `←` 로 되돌아간다. ESC 와 좌우 화살표도 먹는다.

## v0.8.2 — 2026-08-04

### 고침

- **점수는 2,850점인데 순위가 없었다.** 순위 갱신을 *기록이 바뀐 요청*에만 걸어둔 탓이다 — 순위표가 생기기 전에 이미 다 풀어둔 사람은 올릴 기록 변화가 없어서 새 사건을 풀기 전까지 영영 순위 밖이었다. 이제 기록이 그대로여도 순위를 맞춘다. 점수가 안 바뀌었으면 KV 에 다시 쓰지도 않는다.
- **순위를 못 받아온 것과 순위가 없는 것을 구분한다.** 서버가 답을 안 줘도 `아직 순위가 없어` 라고 말해서, 1등인 사람이 자기가 순위 밖인 줄 알았다. 받는 중에는 아무 말도 안 하고, 실패하면 실패했다고 한다.

## v0.8.1 — 2026-08-04

### 고침

- **구형 Safari 에서 파비콘이 아예 없었다.** 아이콘이 SVG 하나뿐이었는데 Safari 는 26.0 부터야 SVG 파비콘을 읽는다(iOS 도 같다) — 그 이전 버전에는 탭에 아무것도 안 떴다. 같은 그림의 64×64 PNG 를 나란히 걸어 대신 가게 했다. 관리 화면도 같다.

## v0.8.0 — 2026-08-04

### 바뀜

- **관리 화면.** `admin.html` 에 플레이어 목록과 기록이 있다. 계정·게스트 기록을 나눠 보여주고, 누르면 그 사람이 푼 사건과 점수가 펼쳐진다. 기록만 지우거나 계정을 통째로 지울 수 있고, 어느 쪽이든 순위표에서도 같이 내려간다.
- **막는 건 페이지가 아니라 서버다.** 정적 호스팅이라 이 HTML 은 누구나 열 수 있다 — 대신 `/adm/*` 가 전부 관리자 토큰을 요구해서, 토큰이 없으면 목록조차 오지 않는다. 토큰(`ADMIN_TOKEN`)은 워커 시크릿이고, **안 넣은 배포는 관리 화면이 아예 안 열린다.**
- 관리 화면은 게임과 다른 번들이다. 라우터를 얹는 대신 Vite 의 다중 진입점을 써서, 관리 코드가 플레이어가 받는 파일에 안 실린다.

## v0.7.0 — 2026-08-04

### 바뀜

- **링크를 공유하면 카드가 뜬다.** 카카오톡에 주소를 붙이면 제목도 그림도 없는 빈 링크로 가던 것 — `index.html` 에 Open Graph 태그가 없었다. 카드 이미지(`public/og.png`)는 앱에서 떠온 진짜 사건 화면이다. 평면도 위에 용의자와 피해자가 놓여 있고, 범행이 일어난 방에 붉은 기가 돈다. 채팅창에서 작게 떠도 읽히도록 제목과 보드만 남겼다.
- **파비콘.** Vite 기본 로고를 쓰고 있었다. 기울어진 평면도 타일에 붉은 X 와 눈이 X 쪽을 보는 용의자 — 16px 에서도 뭉개지지 않게 요소를 셋으로 줄였다.

## v0.6.1 — 2026-08-04

### 바뀜

- **난이도 선택이 하나의 컨트롤이 됐다.** 칩 넷이 나란히 서서 `새 사건` 과 같은 무게로 경쟁하고 상단바의 절반을 먹던 것(535 → 324px). 이제 껍데기 하나 안에 든 알약 넷이고, 고르면 알약이 그 칸으로 미끄러진다. 라벨은 판 크기(`4×4`…`7×7`)로 줄이고 쉬움·보통 같은 말은 툴팁과 스크린리더가 갖는다. 모바일 메뉴도 같은 컴포넌트다.
- 상단바의 `브리핑 ⌄` 화살표가 제 원 안으로 들어갔다. 버튼에 손을 얹으면 껍데기는 올라가고 안쪽 원은 반대로 내려가며 커진다.

## v0.6.0 — 2026-08-03

### 바뀜

- **점수제.** 난이도가 만점을 정하고(4×4 150 · 5×5 250 · 6×6 350 · 7×7 450), 지목이 한 번 늘 때마다 20%씩 깎이고 20%는 남는다. 맞히는 순간 지목 패널에 `+250점` 이 붙는다.
- **점수판.** 모든 유저 TOP 10 과 내 점수·순위가 `⋯` 메뉴(모바일은 `☰`)에 들어간다. 순위에는 계정만 오른다 — 줄을 세우려면 이름이 필요해서다. 게스트도 내 점수는 그대로 보이고, 동기화 서버가 없는 빌드에서도 보인다.
- **정답을 보면 그 사건은 거기서 끝난다.** `정답 보기` 버튼이 누르기 전에 대가를 말하고, 누른 뒤에는 지목이 잠긴다. 다시 감춰도 마찬가지 — 그 사건은 기록에도 점수에도 안 남는다.
- **이미 푼 사건은 다시 풀어도 총점이 안 오른다.** 기록에서 사건을 열어 답을 알고 한 번에 맞히는 길을 막았다 — 사건마다 첫 해결만 친다.

## v0.5.0 — 2026-08-03

### 바뀜

- **매우 어려움 (7×7)** 난이도. 용의자 6명, 방은 7~9개. 생성기는 그대로다 — 한 방에 한 명 제약을 감당할 만큼 방이 나온다.
- **정답 공개가 규칙을 말한다.** 무고한 사람들이 읽는 방향대로 먼저 자리를 잡고, 한 박자 쉰 뒤 피해자 → 범인 순으로 온다. 범인은 살짝 커졌다 제자리로 오고, 마지막에 사건이 벌어진 방에 붉은 기가 돈다. 이 게임의 규칙은 `범인 = 피해자와 같은 방에 있던 용의자` 하나뿐인데, 지금까지는 모두가 똑같이 나타나고 정답은 보드 바깥 문구에만 있어서 이름을 읽고 보드에서 그 글자를 다시 찾아야 했다.
- 사건이 바뀔 때 증언 목록도 보드와 함께 넘어간다. 보드는 페이드로 갈리는데 이름만 제자리에서 바뀌어, 화면의 절반만 움직이는 것처럼 보이던 것.
- 첫 진입에 증언 · 보드 · 지목 세 열이 순서대로 들어온다.
- **가구 그림을 발자국 비율대로 다시 그렸다.** 세 칸짜리 소파와 한 칸짜리 스탠드가 같은 크기로 그려지던 것. 이제 칸마다 제 몫의 물건이 보인다 — 세 칸 소파는 2인용 소파 두 칸 + 스툴 한 칸, 여물통은 칸마다 칸막이, 2×2 건초더미는 둥근 볏단 네 덩이. 정사각 그림을 늘려 쓰지 않으므로 선 굵기도 한 칸짜리 가구와 같다. 범례도 같은 그림이라 목록만 봐도 몇 칸짜리인지 안다.
- **가구가 최대 네 칸까지 놓인다.** 러그·건초더미는 2×2, 소파·여물통·트랙터는 세 칸. 예전엔 두 칸이 최대라 방이 넓어도 평면도가 헐거웠다. (ㄱ자로 꺾인 가구는 아직 없다 — 그림 하나를 눕혀 쓰는 방식이라 꺾인 모양은 회전마다 그림이 따로 있어야 한다)
- **야외에는 창문·문 대신 울타리·대문이 선다.** 마당·목초지·정원·우물가처럼 하늘이 뚫린 칸에 창문이 떠 있었다.

## v0.4.0 — 2026-08-03

### 바뀜

- **데스크톱 화면을 세 열로 다시 짰다.** 증언(= 메모 브러시) · 보드 · 지목/범례가 한 화면에 나란히 선다. 예전에는 사건 카드 → 보드 → 사이드 패널이 세로로 쌓여 있어서, 증언을 읽고 브러시를 고르고 칸을 찍는 세 동작이 800px 넘게 떨어져 있었다. 페이지 높이 1714px(1.9 화면)에서 **한 화면**으로.
- **증언 목록을 누르면 그 인물로 칸을 표시한다.** 모바일이 먼저 쓰던 방식을 데스크톱도 쓴다. 따로 있던 메모 브러시 팔레트는 없앴다 — 증언 줄이 곧 브러시다.
- **범인 지목이 두 번의 클릭으로 끝난다.** 드롭다운을 열고 고르고 누르던 것을 용의자 카드 목록으로 바꿨다. 보드 위 인물 토큰과 같은 모양이라 눈으로 짚기도 쉽다.
- 사건 브리핑·규칙은 상단 사건 제목 버튼이, 시드·기록·계정·피드백은 `⋯` 버튼이 여는 모달로 옮겼다. 한 번 읽고 마는 것들이 늘 자리를 차지할 이유가 없다.
- 넘치는 몫은 페이지가 아니라 열 안에서 스크롤한다. 1366×768 같은 낮은 노트북 화면에서도 페이지 스크롤이 생기지 않는다.

### 고침

- 비활성 상태의 `지목하기` 버튼이 흐린 빨강 바탕에 흰 글씨라 읽기 어렵던 것. 회색 바탕으로 바꿔 대비를 확보했다.

## v0.3.0 — 2026-08-01

### 추가

- **사건을 풀면 인물이 하나씩 자리를 잡는다.** 지금까지는 정답이 한 프레임에 통째로 나타나서, 메모 한 칸 찍는 것과 똑같아 보였다. 퍼즐의 보상 장면이니 여기만 연출을 준다 — 왼쪽 위부터 읽는 방향대로 40ms 간격.
- 새 사건·난이도 변경·시드 열기에서 보드가 부드럽게 갈린다. 4×4 에서 6×6 으로 갈 때 칸 수까지 바뀌어 튀던 것.
- 사건을 해결하면 기록 목록에 새 항목이 위에서 내려온다. 이미 쌓여 있던 기록은 페이지를 열 때 그대로 놓인다 — 매번 우르르 나타나면 그게 더 산만하다.

셋 다 모션을 줄여둔 환경에서는 이동 없이 투명도만 남는다. 메모 찍기·칸 위 마우스처럼 **자주 하는 동작은 여전히 즉시 반응**한다 — 연출은 드문 순간에만 쓴다.

## v0.2.1 — 2026-08-01

### 고침

- **iPhone 에서 바텀시트가 닫힐 때 미끄러져 내려가지 않고 한 프레임에 사라지던 문제.** Chromium 은 정상이라 안 보였다 — `dialog` 가 닫힐 때 `display` 를 미뤄주는 `allow-discrete` 를 WebKit 이 안 지킨다. 이제 두 브라우저가 같은 길로 퇴장한다. ESC 로 닫을 때도 마찬가지다.
- 터치 목표를 키웠다. 증언 줄·닫기 버튼·시트 안의 버튼들이 40px 이하라 오탭이 나던 것을 44px 로 올렸다. 작은 화면(iPhone SE 등)에서는 증언 줄이 34px 까지 줄어들어 있었다.
- 증언 줄에도 누르는 느낌을 넣었다. 옆의 버튼들만 반응해서 증언 줄은 안 눌리는 것처럼 보였다.

### 바뀜

- 기록 목록의 날짜·횟수를 고정폭 숫자로 — 목록이 다시 그려질 때 숫자가 흔들리지 않는다
- macOS 에서 글자를 조금 더 얇고 또렷하게
- 그림 에셋을 붙였을 때 가장자리가 배경에 녹지 않도록 얇은 테두리

## v0.2.0 — 2026-08-01

### 바뀜

- **모바일은 이제 한 화면에 다 들어온다. 스크롤이 없다.** 붙박이는 셋뿐 — 보드, 증언 목록, 하단 액션바. 사건 설명·규칙·범례·지목·난이도·시드·기록은 전부 바텀시트로 들어갔다. 예전에는 페이지가 4화면쯤 됐고 증언은 보드 위, 메모 브러시는 보드 아래라 한 수 둘 때마다 위아래로 스크롤해야 했다.
- **증언 목록이 곧 메모 브러시다.** `Ⓐ 민준 · 난 침대 옆에 있었어!` 줄을 누르면 그 인물로 칸을 표시한다. 증언을 보는 자리에서 바로 찍을 수 있다.
- 보드가 폭뿐 아니라 **높이에도 맞춰** 크기를 정한다. 6×6 도 작은 폰에서 한 화면에 들어온다. 칸 안의 글자·아이콘도 보드 크기를 따라간다.
- 가로로 누운 폰은 보드를 왼쪽에, 증언과 액션을 오른쪽에 둔다.
- 데스크톱 화면은 그대로다.

### 고침

- 노치 기기에서 하단 액션바가 홈 인디케이터에 가리던 문제 (`viewport-fit=cover` + 안전영역)
- `<html lang="en">` → `ko`
- 가구 칸 거절 토스트가 좁은 화면에서 한 줄로 잘리던 문제
- 칸을 연속으로 누를 때 iOS 더블탭 확대 때문에 반응이 밀리던 문제
- 스타일을 읽는 테스트가 조용히 빈 문자열을 보고 통과하던 문제 (Vitest 의 `css: false` 가 `?raw` 까지 스텁으로 바꾼다)

## v0.1.4 — 2026-07-30

### 바뀜

- 소스 파일을 500줄 아래로 유지하도록 컴포넌트 단위로 쪼갰다. 스타일은 `src/styles/*.css` 로, 테스트는 검사 대상별로, 기록·계정 패널은 `HistoryPanel` 로 나왔다. 규칙은 테스트가 강제한다.

## v0.1.3 — 2026-07-30

### 추가

- 아이디/비밀번호 계정 — 다른 기기에서 로그인하면 기록이 따라온다. **로그인은 선택이고, 안 해도 게임과 로컬 기록은 그대로 굴러간다.** 로그인 없이 풀던 기록은 가입하는 순간 계정으로 합쳐진다.
- 비밀번호는 브라우저에서 PBKDF2-SHA256 31만 회로 늘려 보내므로 서버가 원문을 보지 않는다.

### 바뀜

- 기록 코드를 **복구 키**로 다시 이름 붙였다. 비밀번호 재설정이 없으니(이메일을 안 받는다) 코드를 적어둔 사람이 되찾을 수 있다.
- 로그인 중에는 `이 기기에서 기록 지우기` 를 감춘다. 로그아웃과 같은 동작인데 계정 사본이 남아 "지우기"가 사실이 아니게 된다.

## v0.1.2 — 2026-07-30

### 추가

- 푸터의 버전 번호를 누르면 이 버전 기록을 보여주는 모달
- 플레이 기록과 기기 간 동기화 — 해결한 사건이 쌓이고, 코드를 넣으면 다른 기기와 합쳐진다
- 사건 현장 표현 확장 — 테마별 방·바닥 재질·직업, 비직사각형 방, 가구 아이콘
- 피드백 모달 — 시드·난이도·버전이 채워진 GitHub 이슈 작성 폼을 연다
- 모션 토큰과 모달 등장·퇴장 애니메이션

### 고침

- Safari 에서 난이도를 바꿨다 되돌아오면 6×6 보드가 4열 폭으로 잘리던 문제 ([webkit#202259](https://bugs.webkit.org/show_bug.cgi?id=202259))
- 터치 기기에서 hover 효과가 눌러붙던 문제
- 같은 칸·같은 용의자를 다시 눌러도 거절·판정 모션이 다시 재생되도록
- `prefers-reduced-motion` 에서 필요한 피드백까지 죽던 문제

## v0.1.1 — 2026-07-30

### 추가

- GitHub Pages 자동 배포
- 가구를 방 이름에 맞게 배치해 빈 방을 없앤다
- 가구 칸에 메모하려 하면 흔들림 + 안내 토스트

## v0.1.0 — 2026-07-30

- 첫 공개. 증언을 조합해 피해자와 같은 방에 있던 용의자를 지목하는 퍼즐, 난이도 4·5·6.
